<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans 
       http://www.springframework.org/schema/beans/spring-beans.xsd 
       http://www.springframework.org/schema/mvc 
       https://www.springframework.org/schema/mvc/spring-mvc.xsd 
       http://www.springframework.org/schema/context 
       https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<!-- <annotation-driven /> -->

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<!-- <resources mapping="/resources/**" location="/resources/" /> -->

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>
	
	<context:component-scan base-package="com.rio.base" />
	
	<!-- 아래 태그는 Spring MVC가 @Controller에 요청을 보내기 위해 HandlerMapping 과 HandlerAdapter 를 bean 으로 등록한다. -->
	<!-- 이렇게 등록된 bean 에 의해 요청 url 과 컨트롤러를 매칭할 수 있다. -->
	<!-- 근본적으로 @Controller 없이는 이 태그는 아무것도 하지 않는다고 할 수 있다. -->
	<mvc:annotation-driven conversion-service="conversionService"></mvc:annotation-driven>
	
	<!-- 위에 <resource> 와 태그만 좀 다르고 내용은 비슷하다. -->
	<!-- location에 대한 요청이 들어오면 WAS의 DefaultServlet에 위임한다. -->
	<!-- 즉, 모든 요청은 Spring MVC에 영향을 받아 직접접근이 불가능하므로 요청을 WAS로 하면서 URL을 통해 resource에 접근하는것이다! -->
	<!-- 이미지나 html 파일과 같은 정적인 파일의 경로를 지정한것이다. -->
	<!-- /resources 경로로 들어오는 요청을 정적파일을 요구하는 것이라고 생각하고 스프링 MVC에서 처리하지 않는다는 의미이다. -->
	<mvc:resources location="/resources/" mapping="/resources/**"></mvc:resources>
	
	<!-- Formatter -->
	<!-- conversionService 라는 빈을 등록한 후에는 스프링 MVC를 처리할 때 <mvc:annotation-driven> 에 이를 이용한다는 것을 지정해야만 한다. -->
	<bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
		<property name="formatters">
			<set>
				<bean class="com.rio.base.controller.formatter.LocalDateFormatter" />
			</set>
		</property>
	</bean>
	
	
	
</beans>
